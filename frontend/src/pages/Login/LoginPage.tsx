import { Container, Title, Text } from '@mantine/core';

export function LoginPage() {
  return (
    <Container size="xs" py="xl">
      <Title order={1}>Login</Title>
      <Text c="dimmed">Authentication page — coming soon.</Text>
    </Container>
  );
}
