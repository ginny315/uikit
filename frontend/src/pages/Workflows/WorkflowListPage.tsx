import { Container, Title, Text } from '@mantine/core';

export function WorkflowListPage() {
  return (
    <Container size="xl" py="xl">
      <Title order={1} mb="md">Workflows</Title>
      <Text c="dimmed">Workflow list — coming in Session 7.</Text>
    </Container>
  );
}
