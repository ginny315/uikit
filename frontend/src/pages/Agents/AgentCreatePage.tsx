import { Container, Title, Text } from '@mantine/core';

export function AgentCreatePage() {
  return (
    <Container size="xl" py="xl">
      <Title order={1} mb="md">Create Agent</Title>
      <Text c="dimmed">Agent creation form — coming in Session 4.</Text>
    </Container>
  );
}
