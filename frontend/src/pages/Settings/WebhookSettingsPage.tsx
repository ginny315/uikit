import { Container, Title, Text } from '@mantine/core';

export function WebhookSettingsPage() {
  return (
    <Container size="xl" py="xl">
      <Title order={1} mb="md">Webhooks</Title>
      <Text c="dimmed">Webhook configuration — coming in Session 8.</Text>
    </Container>
  );
}
