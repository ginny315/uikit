import { Container, Title, Text } from '@mantine/core';

export function AccessSettingsPage() {
  return (
    <Container size="xl" py="xl">
      <Title order={1} mb="md">Access Control</Title>
      <Text c="dimmed">User and role management — coming in Session 8.</Text>
    </Container>
  );
}
